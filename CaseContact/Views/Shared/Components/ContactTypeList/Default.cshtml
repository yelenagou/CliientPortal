@model IEnumerable<CaseContact.Models.ContactType>

<h2>View</h2>


<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ContactTypeList)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ContactTypeList)
                </td>
                <td>
                   
                  
                    <a asp-action="Edit" id="editButton" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
<div class="dvCategoryResults">

</div>
@section Scripts {

<script type="text/javascript">
    $(document).ready(function () {
      

        $("#editButton").click(function (e) {
            e.preventdefault();
            var url = $(this).data(url);



            );
        }
    });
</script>
}